# Database (PostgreSQL)
# Set these in your shell or with conda:  conda env config vars set DB_USER=postgres ...
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=lepra

# JWT (optional; if not set, a default is used - change for production)
# SECRET_KEY=your-secret-key-here
